Closes the page with one line of intent and a large brass mailto — no form, no button.

```jsx
<ContactBlock line="If the next drop needs a point of view, write." email="bilal@example.com"
  links={[{label:'Instagram',href:'#'},{label:'Full reel',href:'#'}]} />
```
